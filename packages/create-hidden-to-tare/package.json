{
  "name": "create-hidden-to-tare",
  "version": "0.1.1",
  "description": "Initialize the exchangeable development workflow in a new Git repository.",
  "type": "module",
  "bin": {
    "hidden-to-tare": "dist/cli.js",
    "create-hidden-to-tare": "dist/cli.js"
  },
  "files": ["dist", "templates", "package.json"],
  "scripts": {
    "build": "node ../../node_modules/typescript/bin/tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
