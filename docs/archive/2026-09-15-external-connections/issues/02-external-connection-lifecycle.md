---
status: done
---

# 02: 外部接続を安全に準備・撤去し、未移管データを残した完了を拒否する

## 実現する振る舞い

[変更仕様](../../2026-09-15-matt-connection-isolation/spec.md)のQ1〜Q8・Q10に従い、外部接続ごとに専用領域を持ち、
固定パスの互換ファイルも含めて準備・撤去できるようにする。
保存すべきか不明なデータを勝手に消さず、未移管データを残した完了を止める。
任意の接続定義を扱う共通の仕組みとし、Matt固有の移行は03で行う。

## 依存チケット

なし（すぐに着手可能）。検証ログの簡潔化を前提にしない。

## 受け入れ条件

- [x] Git管理する再生成用の接続定義から、接続別のGit管理外領域を明示CLIで準備できる。
- [x] 固定パスの互換ファイルも接続側の所有対象として管理し、専用領域外の例外を撤去時に取り残さない。
- [x] 複数の独立した接続を併用できる。既存ファイルや他接続との固定パス競合は拒否し、元の内容を保持する。
- [x] 撤去は接続側が生成し、生成後に変更されていないと確認できる管理ファイルだけを対象にする。
- [x] 未移管・変更済み・所有不明のデータが残っていれば、診断付きで撤去を拒否し、データを保持する。
- [x] 完了時は同じworktree内の全接続を検査し、未移管データがあれば拒否する。別worktreeへ干渉しない。
- [x] 作業データを共通側へ移管、または明示的に破棄した後は、他の条件を満たせば完了・撤去できる。意味的な自動移管・自動破棄はしない。
- [x] 再生成できる未変更の接続ファイルは完了時にも残せる。調査・実装中の資料の存在だけで通常検証を一律に止めない。
- [x] 接続が一つもなくても共通の検証・完了判定が動く。通常検証は接続用ファイルを生成・撤去しない。
- [x] 不正な定義や所有記録を成功扱いせず、他接続、共通成果物、worktree外のデータを上書き・撤去しない。
- [x] 分類、必要成果物、検証証跡、独立レビュー、ADR本文承認、最終SHA承認、Git状態、検証中の変更検出とfinalizeの記録保持を維持する。
- [x] 一時Gitリポジトリの接続・撤去・verify・finalize CLIで、準備から撤去までの正常系と上記の拒否・保持を確認する。
- [x] 現行仕様と利用案内に、領域・コマンド・所有記録の扱いを明文化する。物理的なサンドボックスと説明しない。
- [x] SKILL本体のインストール・更新・改造を行わず、独立レビュー・完了文書整理後の通常検証を成功させる。

## 検証結果

`scripts/workflow/connections.test.ts`の39件は、実際の一時Gitリポジトリから
接続・撤去・通常verify・finalizeを呼び、終了コード・診断・ファイルの保持と削除を確認する。
正常系、複数接続の競合、未移管データの拒否と整理後の再試行、不正JSON・パス・UTF-8、
所有記録の破損・欠落・変更、追跡ファイル、シンボリックリンク・ハードリンク、
別worktree、残存ロック、検証中の所有記録・生成ファイル削除を含む。
診断ログは未移管データと混同せず、未変更の接続を残した完了を確認した。

`npm run verify`で型検証・全215テスト・完了契約・ADR・Markdown・ローカルリンクを確認した。
既存176テストも維持し、分類・証跡・レビュー・ADR承認・最終SHA・Git状態・finalize保持の回帰を確認した。
独立レビューは`main (0d2d8fd)`を比較元に、規約・仕様の2軸で実施した。

- Standards: ルート直下の互換ファイル生成、空定義での専用領域置換の2指摘を修正。
  未使用引数の改善候補も解消し、修正差分の再確認で残存指摘なし。
- Spec: ルート直下の互換ファイル生成の1指摘（上記と同じ）を修正。再確認で残存指摘なし。
- 上記2不具合はCLIテストの失敗を先に確認し、修正後に成功を確認した。
  SKILL本体・採用済みADR本文は変更していない。

共通仕様と未完了の03は作業場所に維持する。02は接続分離の基盤であり、
文書読込量の削減は03で測定する。02だけで実token消費の削減率を主張しない。
完了契約と撤去処理に関わるため軽微変更として扱わず、最終SHAへの人間承認とfinalizeは
一時マニフェストで別途管理する。チケットのdoneは最終SHA承認の代わりではない。
