---
status: done
---

# 01: ルート移動後の検証パスを修復する

## 実現する振る舞い

検証コマンドが実行場所に依存せず、現在のリポジトリルートにある文書、ADR、
完了チケット、依存ツールを検査する。

## 依存チケット

なし（すぐに着手可能）

## 受け入れ条件

- [x] 完了チケット検査が現在のリポジトリルートを対象にする
- [x] ADR検査が現在の`docs/adr/`を対象にする
- [x] Markdownとリンク検査が現在のパスと依存ツールを使用する
- [x] テストfixtureが現在のルート構造を再現する

## 検証結果

- `npm run verify`でルート構成に対する全検証が成功した。
- 既存38テストを修復直後に実行し、すべて成功した。
